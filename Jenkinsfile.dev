pipeline {
    agent any
    environment {
        APP_ENV = "staging"

    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                echo "${APP_ENV}"
            }
        }
        stage('learn credentials') {
            steps{
             echo 'learn credentials'
              echo "${APP_ENV}"
            }

        }

        stage('build') {
            steps {
                echo 'The second stage'
                echo 'The second steps'
            }
        }
    }
}
